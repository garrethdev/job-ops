# Modularization + Testing plan (multi-agent review, 2026-07-10)

## Modularization (ranked)
1. [S] Wire a JS test runner: add "scripts":{"test":"node --test"} to vercel-app/package.json (suites in vercel-app/test/*.test.js, auto-discovered) and add a node-18 js-test job to .github/workflows/ci.yml alongside the existing pytest job.
2. [S] Extract the pure helpers out of vercel-app/lib/gmail.js (mime, b64url, looksLikeEmail, oneLine) into a new vercel-app/lib/mime.js and export them; gmail.js imports them and keeps only accessToken/gfetch/ensureLabel/createLabeledDraft. No behavior change.
3. [M] Extract vercel-app/public/app.js pure helpers (esc, safeUrl, safeEmail, laneLabel, scoreClass, LANE_LABELS) into vercel-app/public/format.js as named exports; switch index.html to <script type="module"> and move the ~6 inline onclick handlers to addEventListener wiring or explicit window.* assignments so the markup keeps working.
4. [S] Extract `export function parseEmailJSON(text) -> {subject, body}` (brace-slice + JSON.parse + subject/body completeness check + slice(0,150)/slice(0,4000) clamps) from api/draft.js craftEmail into vercel-app/lib/llm.js; craftEmail keeps only the fetch and calls it.
5. [M] Fix the id/dedupe drift: port Python normalize_company (INCLUDING the legal-suffix strip inc|llc|ltd|corp|co|gmbh) and normalize_title byte-for-byte into a shared vercel-app/lib/id.js, and have find.js call it instead of its ad-hoc norm() (which does NOT strip suffixes). Lock parity with a committed golden-vector fixture.
6. [M] Extract `buildLeadRow({company,title,lane,contact,now})` (plus norm/sha1) from api/find.js into vercel-app/lib/lead_row.js (pure, exported), reusing lib/id.js; the handler calls findContact -> buildLeadRow -> insertLead.
7. [S] Make `snippet` a first-class optional schema field: add snippet="" as a kwarg to core/schema.new_record() and emit it in the dict; document it (or add to REQUIRED_FIELDS as empty-allowed). Remove the post-construction rec["snippet"]=... bolt-ons in modules/web_checker/run.py:44 and modules/email_scanner/run.py:95.
8. [S] Constrain api/find.js to the real enum contract: stop writing source:'manual' and lane:(lane||'lookup'), which core/config.py forbids (SOURCES, LANES) and core/schema.validate() would reject. Either add 'manual' to SOURCES as a versioned decision, or map to a valid source; reject/normalize any client lane not in LANES instead of defaulting to 'lookup'.
9. [S] Fix the check_coupling.py relative-import blind spot: resolve `from ..sibling import x` (level>=2) against the file's package path and apply the sibling/core rules, instead of unconditionally `continue`-ing on every relative import (lines 44-45, 57-58).
10. [L] Generate a committed vercel-app/lib/contract.json from Python (SCHEMA_VERSION, COLS, SOURCES, STATUSES, LANES, TITLES_BY_LANE, FALLBACK_TITLES, seniority list); find.js imports SCHEMA_VERSION+enums from it, scripts/sync_to_supabase.py reads COLS from it, and a CI check re-runs the generator and fails if the committed file is stale (same pattern as check_coupling.py).
11. [M] Extract a shared HTTP helper (core/http.py) exposing post_json(url,headers,payload,timeout) and get_json(...); have core/apollo.py (_post), core/gmail.py (_access_token/_get) and core/scoring.py (_post_json) call it.
12. [S] Memoize profile keyword loading: wrap core/profile.load_keywords/load_profile_text in functools.lru_cache (or a module dict mirroring scoring._PAT_CACHE).
13. [M] Extract Apollo pure decision helpers pickTitles(lane) and shapeContact(person, candidate) in BOTH core/apollo.py and vercel-app/lib/apollo.js; the async findContact keeps only orchestration. Accept the dual-port control-flow duplication (a Python cron + a JS serverless fn is legitimate) but move the shared DATA (TITLES_BY_LANE/FALLBACK_TITLES/seniorities) into contract.json (item 10).
14. [L] Introduce an explicit integration layer (core/clients/ with gmail.py, apollo.py, llm.py — or a top-level integrations/ package that modules may import but which never imports a module) and move gmail, apollo, and scoring's LLM backend there; leave core/ as the pure domain layer. Update core/__init__.py so its stated contract matches contents.
15. [S] Fix the stale 2-lane docstrings/comments now that there are 4 lanes: core/profile.py:3-4, core/scoring.py:1, the wrong tie-break comment at scoring.py:74 (max() returns first lane = 'gtm-engineer', not 'architecture'), modules/apply_pipeline/__init__.py:6, contact_enricher/__init__.py:7, and core/__init__.py:1.

## Do now
- Wire the JS runner: add "scripts":{"test":"node --test"} to vercel-app/package.json (zero deps). This is the gate for every JS test below.
- Extract vercel-app/lib/mime.js (mime, b64url, looksLikeEmail, oneLine) from lib/gmail.js and write vercel-app/test/gmail.test.js covering CRLF/Bcc header-injection rejection, To-omitted-on-invalid-address, looksLikeEmail accept/reject, and b64url URL-safety. (security-critical helper that just had bugs)
- Extract vercel-app/public/format.js (esc, safeUrl, safeEmail, laneLabel, scoreClass) from public/app.js and write vercel-app/test/format.test.js covering safeUrl blocking javascript:/data:/protocol-relative, safeEmail accept/reject, and esc escaping &<>". (security-critical XSS helpers)
- Extract vercel-app/lib/llm.js parseEmailJSON(text) from api/draft.js and write vercel-app/test/llm.test.js covering clean/fenced/prose-wrapped JSON, inner-brace body, missing subject|body throw, and 150/4000 clamps. (cheap extraction that unlocks the most failure-prone logic)
- Extend tests/test_store.py with get_record/update_record/delete_record cases — including that an invalid update (status='enriched' with empty lane) raises SchemaError and leaves the file byte-unchanged. Biggest Python data-integrity gap and needs NO refactor.
- Make snippet a first-class optional field in core/schema.new_record(), remove the two rec['snippet']=... bolt-ons in web_checker/run.py and email_scanner/run.py, and extend tests/test_schema.py to pin it. Cheap modularization win that closes a real out-of-schema cross-module contract.
